pipeline{

agent {label “maven-agent”}
   stages{
      stage('build'){
          echo "Building applications"
      }
      stage('deploy'){
          echo "Deploying applications"
      }
   
   }
}
